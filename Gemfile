source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# Ruby 3.4+ removed several gems from defaults that Jekyll still expects.
# Pin them explicitly so `bundle exec jekyll serve` works on modern Ruby.
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
